#!/usr/bin/env sh

echo "Running pre-commit checks..."

# Frontend: Run lint-staged for staged files only
# This includes ESLint (with custom no-tailwind-base-colors rule), Prettier, and Stylelint
echo "Checking frontend staged files..."
cd frontend
npx lint-staged || exit 1
cd ..

echo "All pre-commit checks passed!"
