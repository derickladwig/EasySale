# Stripe Connect (EasySale platform credentials)
STRIPE_CLIENT_ID=ca_xxx
STRIPE_SECRET_KEY=sk_xxx
STRIPE_REDIRECT_URI=https://YOURDOMAIN/api/integrations/stripe/callback

# Phase 2 - webhooks
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Optional: environment guard (example)
APP_ENV=development   # or production
